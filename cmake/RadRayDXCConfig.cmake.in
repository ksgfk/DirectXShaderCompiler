@PACKAGE_INIT@

set(RadRayDXC_VERSION "@RADRAY_DXC_PACKAGE_VERSION@")
set(RadRayDXC_Headers_FOUND TRUE)
set(RadRayDXC_Compiler_FOUND TRUE)
set(RadRayDXC_Validator_FOUND FALSE)
set(RadRayDXC_CLI_FOUND FALSE)

if (NOT TARGET RadRayDXC::Headers)
  add_library(RadRayDXC::Headers INTERFACE IMPORTED)
  set_target_properties(RadRayDXC::Headers PROPERTIES
    INTERFACE_INCLUDE_DIRECTORIES "${PACKAGE_PREFIX_DIR}/include")
endif()

if (NOT TARGET RadRayDXC::Compiler)
  add_library(RadRayDXC::Compiler SHARED IMPORTED)
  set_target_properties(RadRayDXC::Compiler PROPERTIES
    IMPORTED_IMPLIB "${PACKAGE_PREFIX_DIR}/lib/dxcompiler.lib"
    IMPORTED_LOCATION "${PACKAGE_PREFIX_DIR}/bin/dxcompiler.dll"
    INTERFACE_LINK_LIBRARIES RadRayDXC::Headers)
endif()

if (EXISTS "${PACKAGE_PREFIX_DIR}/bin/dxil.dll" AND
    EXISTS "${PACKAGE_PREFIX_DIR}/lib/dxil.lib" AND
    NOT TARGET RadRayDXC::Validator)
  add_library(RadRayDXC::Validator SHARED IMPORTED)
  set_target_properties(RadRayDXC::Validator PROPERTIES
    IMPORTED_IMPLIB "${PACKAGE_PREFIX_DIR}/lib/dxil.lib"
    IMPORTED_LOCATION "${PACKAGE_PREFIX_DIR}/bin/dxil.dll"
    INTERFACE_LINK_LIBRARIES RadRayDXC::Headers)
  set(RadRayDXC_Validator_FOUND TRUE)
endif()

if (EXISTS "${PACKAGE_PREFIX_DIR}/bin/dxc.exe" AND
    NOT TARGET RadRayDXC::CLI)
  add_executable(RadRayDXC::CLI IMPORTED)
  set_target_properties(RadRayDXC::CLI PROPERTIES
    IMPORTED_LOCATION "${PACKAGE_PREFIX_DIR}/bin/dxc.exe"
    INTERFACE_LINK_LIBRARIES RadRayDXC::Compiler)
  set(RadRayDXC_CLI_FOUND TRUE)
endif()

check_required_components(RadRayDXC)
